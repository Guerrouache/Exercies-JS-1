let nombre = prompt ("remplir un nombre a virgule");
let nombre2 = prompt ("remplir un nombre a virgule");



function multiplier(nb,nb2) {
   return (Math.trunc(nb)*nb2);
   
}

alert(multiplier(nombre, nombre2));